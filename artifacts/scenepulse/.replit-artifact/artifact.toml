kind = "web"
previewPath = "/"
title = "ScenePulse"
version = "1.0.0"
id = "artifacts/scenepulse"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24809

[services.development]
run = "pnpm --filter @workspace/scenepulse run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/scenepulse", "run", "build" ]
publicDir = "artifacts/scenepulse/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24809"
BASE_PATH = "/"
